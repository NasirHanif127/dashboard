{
  "name": "dashboard",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run build --prefix client",
    "start": "npm install && npm run build --prefix client && node server/app.js",
    "dev": "concurrently \"npm start --prefix client\" \"npm start --prefix server\""
  },
  "dependencies": {
    "concurrently": "^5.3.0"
  }
}
